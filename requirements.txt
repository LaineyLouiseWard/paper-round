requests>=2.28
pyyaml>=6.0
anthropic>=0.40  # only needed for local screening runs
# feedparser is installed separately with --no-deps (its sgmllib dep is vendored
# under vendor/ — sgmllib3k is source-only and fails to build in the cloud sandbox):
#   pip install --no-deps feedparser
